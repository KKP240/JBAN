<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/navbar.css" />
    <script defer src="/js/utilities.js"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com/" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Kanit:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/edit_product.css">
    <link rel="stylesheet" href="/css/footer.css">
    <script defer src="/js/editProduct.js"></script>
</head>
<body>
    <%- include("nav_res"); %>
    <%- include("navbar"); %>

    <div class="container" data-id="<%= product._id %>">
        <div class="topic">แก้ไขสินค้า</div>

        <div class="add-container">
            <div class="left">
                <img id="previewImage" src="images/black-tshirt.jpg">
            </div>

            <div class="right">
                <form id="edit-form">
                    <div class="product-name">
                        <label for="pName">ชื่อสินค้า</label>
                        <input type="text" name="name" id="pName" placeholder="เปลี่ยนชื่อสินค้า" required>
                    </div>
                    <div class="category-gender">
                        <div class="gender">เพศ</div>
                        <input type="radio" name="category" id="male" value="men" checked><label for="male" >ชาย</label>
                        <input type="radio" name="category" id="female" value="women"><label for="female">หญิง</label>
                    </div>
                    <div class="category-type">
                        <label for="pType">ประเภท</label><br>
                        <input type="text" name="type" id="pType" placeholder="เปลี่ยนประเภทของสินค้า" required>
                    </div>
                    <div class="describe">
                        <label for="pDescribe">คำอธิบาย</label>
                        <textarea name="description" id="pDescribe" placeholder="เปลี่ยนคำอธิบายของสินค้า" required></textarea>
                    </div>
                    <div class="price">
                        <label for="pPrice">ราคา</label>
                        <input type="number" name="price" id="pPrice" min="0" placeholder="เปลี่ยนราคาของสินค้า" required>
                    </div>
                    
                    <div class="details-container">
                        <% product.variants.forEach((v, i) => { %>
                            <div class="repeated-details">
                                <div class="color">
                                    <label for="<%= v.color%>">สี <span style="color: #00aaff;">(สีเก่า <%= v.color %>)</span></label>
                                    <input type="text" name="variants" data-value="<%= v.color%>" id="<%= v.color%>" placeholder="ใส่สีใหม่" required>
                                </div>
                                <div class="size">
                                    <% v.sizes.forEach(s => { %>
                                        <div class="size-details">
                                            <label for="<%= v.color%>-<%= s.size %>"><%= s.size %></label>
                                            <input type="text" id="<%= v.color%>-<%= s.size %>" name="sizes" data-value="<%= s.size %>" data-color="<%= v.color %>" placeholder="ใส่จำนวนสินค้า" required>
                                        </div>
                                    <% }) %>
                                </div>
                            </div>
                        <% }) %>
                        </div>
                    <button class="submit-edit" type="submit">แก้ไขสินค้า</button>
                    </div>
                </form>
        </div>
    </div>
    <%- include("footer"); %>
</body>
</html>